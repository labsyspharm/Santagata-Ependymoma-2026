## Environment Setup

This project was developed using a Conda environment named `sc`.

Activate the environment before running the scripts:

```bash
conda activate sc
```

If the environment has not been installed yet, recreate it using:

```bash
conda env create -f environment.yml
```
